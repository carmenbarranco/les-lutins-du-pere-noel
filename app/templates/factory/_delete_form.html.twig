<form method="post" action="{{ path('factory_delete', {'id': factory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ factory.id) }}">
    <button class="btn">Delete</button>
</form>
